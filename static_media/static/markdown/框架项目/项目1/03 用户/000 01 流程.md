```
# 接收数据
username = request.POST.get("username")
# 校验完整性
if not all([])
# 登陆校验内置认证系统
# 激活校验
# 判断是否需要next跳转

# 登陆装饰器
# 定义一个类
# 类视图继承此类

# request.user直接传递给模板
# request.is_authenticated


```

