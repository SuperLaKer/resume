请求上下文

```
from flask import Flask, request, session, set_session
# flask上下文
# 线程局部变量
# 把request当做为一个全局的大字典，线程编号作为key，内容为前端的数据
```

应用上下文

```
# 一次请求多个函数间共享这个g变量

仅在代码执行时，存储这段代码的数据
跟请求没关系
看似全局变量，但是在函数中相当于一个局部变量，函数中调用另一个函数，这个变量也会传过去
当另一个用户访问时，代码又执行一边，这个变量也不一样了
```

