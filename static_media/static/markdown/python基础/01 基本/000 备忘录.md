- 变量  和  数据都是分开保存在内存中的
- 在python中，函数的 参数、返回值都是靠引用传递的
- 函数内部使用赋值语句
- 函数内部使用方法
- 列表（+=）相当于extend
- list1.extend(list2),整合（地址不变）
- 交换数字
- +=
- 排序list.sort(reverse=True)（默认值缺省参数）
- 定义参数给形参赋值
- 调用函数时不用传参数，也可以传递参数
- 给函数变量添加说明（小黄灯泡）
- 缺省参数放到最后
- demo(1, 2, 3, 4, name='xiaoming', age=18)
- dir(对象名字)：列出对象所有方法
- 类表生成式

