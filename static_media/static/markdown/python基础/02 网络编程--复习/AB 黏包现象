```
ret = os.popen()
```

#### 黏包问题

- 首先只有在tcp协议中才会出现黏包现象
- 因为tcp是面向流的协议
- 在发送数据传输的过程中还有缓存机制来避免数据丢失
- 因此再发送小数据的时候，以及接收大小不符合的时候都容易出现黏包现象
- 本质还是因为我们在接收数据的时候不知道发送数据的长度len()
- 接收小于发送，切连续发送

#### 解决黏包问题

- 在传输大量数据前先告诉接收端发送数据的大小
- 通过struct模块来定制协议

#### struct模块

